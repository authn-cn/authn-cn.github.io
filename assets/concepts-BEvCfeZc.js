import{b as e,f as t,h as n,m as r,p as i,t as a,u as o,v as s,x as c}from"./app-D04jC65z.js";var l=JSON.parse(`{"path":"/en/webauthn/concepts.html","title":"Core Concepts","lang":"en-US","frontmatter":{"title":"Core Concepts","description":"Core Concepts This page clarifies terminology and models that appear repeatedly in WebAuthn. It is recommended to read the first, then this page, and finally review the and . Pu...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Core Concepts\\",\\"image\\":[\\"\\"],\\"dateModified\\":\\"2026-07-06T07:49:59.000Z\\",\\"author\\":[]}"],["meta",{"property":"og:url","content":"https://authn.tech/en/webauthn/concepts.html"}],["meta",{"property":"og:site_name","content":"Authn.tech"}],["meta",{"property":"og:title","content":"Core Concepts"}],["meta",{"property":"og:description","content":"Core Concepts This page clarifies terminology and models that appear repeatedly in WebAuthn. It is recommended to read the first, then this page, and finally review the and . Pu..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"en-US"}],["meta",{"property":"og:locale:alternate","content":"de-DE"}],["meta",{"property":"og:locale:alternate","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2026-07-06T07:49:59.000Z"}],["meta",{"property":"article:modified_time","content":"2026-07-06T07:49:59.000Z"}],["link",{"rel":"canonical","href":"https://authn.tech/en/webauthn/concepts.html"}],["link",{"rel":"alternate","hreflang":"de-de","href":"https://authn.tech/de/webauthn/concepts.html"}],["link",{"rel":"alternate","hreflang":"zh-cn","href":"https://authn.tech/webauthn/concepts.html"}]]},"git":{"updatedTime":1783324199000,"contributors":[{"name":"linux","username":"linux","email":"lich.wang@gmail.com","commits":1,"url":"https://github.com/linux"},{"name":"Claude Opus 4.8","username":"","email":"noreply@anthropic.com","commits":1}],"changelog":[{"hash":"3d03564154465dae2b1663376ea15a4f48fb60dd","time":1783324199000,"email":"lich.wang@gmail.com","author":"linux","message":"i18n:站点三语(中/英/德)+ 语言切换 + 浏览器语言默认","coAuthors":[{"name":"Claude Opus 4.8","email":"noreply@anthropic.com"}]}]},"filePathRelative":"en/webauthn/concepts.md","autoDesc":true}`),u={name:`concepts.md`};function d(a,l,u,d,f,p){let m=e(`RouteLink`);return s(),t(`div`,null,[l[13]||=o(`h1`,{id:`core-concepts`,tabindex:`-1`},[o(`a`,{class:`header-anchor`,href:`#core-concepts`},[o(`span`,null,`Core Concepts`)])],-1),o(`p`,null,[l[3]||=r(`This page clarifies terminology and models that appear repeatedly in WebAuthn. It is recommended to read the `,-1),n(m,{to:`/en/webauthn/`},{default:c(()=>[...l[0]||=[r(`Overview`,-1)]]),_:1}),l[4]||=r(` first, then this page, and finally review the `,-1),n(m,{to:`/en/webauthn/flows.html`},{default:c(()=>[...l[1]||=[r(`Flows`,-1)]]),_:1}),l[5]||=r(` and `,-1),n(m,{to:`/en/webauthn/reference.html`},{default:c(()=>[...l[2]||=[r(`Reference`,-1)]]),_:1}),l[6]||=r(`.`,-1)]),l[14]||=i(`<h2 id="public-key-credential-model" tabindex="-1"><a class="header-anchor" href="#public-key-credential-model"><span>Public Key Credential Model</span></a></h2><p>The core of WebAuthn is <strong>asymmetric key pairs</strong>. Whenever a user registers an account on a <strong>Relying Party (RP)</strong>, the authenticator generates a <strong>brand new pair of keys</strong>:</p><ul><li><strong>Private key</strong>: Stored within the secure hardware of the <strong>Authenticator</strong>, <strong>never leaves the authenticator</strong>, and neither the RP nor the browser can access it.</li><li><strong>Public key</strong>: Returned to the RP and stored bound to the user&#39;s account.</li></ul><p>Key properties:</p><ul><li><strong>One RP, one account, one key pair</strong>: Keys are isolated by <code>(rpId, user account)</code>, making credentials from different sites unrelated and unable to be used for cross-site tracking.</li><li><strong>Login via signature</strong>: During authentication, the authenticator signs the challenge issued by the RP using the private key; the RP verifies the signature using the stored public key. The server never stores any secret usable for login.</li></ul><div class="hint-container tip"><p class="hint-container-title">Tips</p><p>This is why WebAuthn is resistant to server compromise: the database only contains the public key; if breached, attackers can neither forge signatures nor derive the private key.</p></div><h2 id="credential-id-and-credentialpublickey" tabindex="-1"><a class="header-anchor" href="#credential-id-and-credentialpublickey"><span>Credential ID and credentialPublicKey</span></a></h2><p>After successful registration, the RP needs to store two things:</p><ul><li><strong>Credential ID</strong>: A unique handle assigned by the authenticator to this key pair (a byte string, commonly stored as Base64URL-encoded). During authentication, the RP returns it via <code>allowCredentials</code> so the authenticator can locate the corresponding private key.</li><li><strong>credentialPublicKey</strong>: The user&#39;s public key, encoded in <strong>COSE (CBOR Object Signing and Encryption) Key</strong> format (RFC 8152). It is a CBOR map containing key type (<code>kty</code>), algorithm (<code>alg</code>, such as -7 for ES256), and curve/coordinate or modulus parameters.</li></ul><p>The RP server typically parses the public key and persists it in a form convenient for signature verification, such as PEM or raw COSE.</p><h2 id="attestation-and-assertion" tabindex="-1"><a class="header-anchor" href="#attestation-and-assertion"><span>attestation and assertion</span></a></h2><p>These are two easily confused words with completely different meanings:</p><ul><li><strong>attestation (Attestation)</strong>: Occurs during the <strong>registration ceremony</strong>. The authenticator, when returning the new public key, can attach a verifiable claim stating &quot;what model/vendor this authenticator is&quot; (<code>attestationObject</code>), allowing the RP to verify the authenticator&#39;s <strong>origin and trustworthiness</strong>.</li><li><strong>assertion (Assertion)</strong>: Occurs during the <strong>authentication ceremony</strong>. The authenticator&#39;s <strong>signature result</strong> for the challenge, used to prove to the RP that the user currently possesses the corresponding private key.</li></ul><p>Attestation format is identified by the <code>fmt</code> field; common types:</p><table><thead><tr><th>fmt</th><th>Meaning</th></tr></thead><tbody><tr><td><code>none</code></td><td>No attestation provided; authenticator origin is unverifiable. <strong>Recommended for most consumer scenarios</strong>, best privacy.</td></tr><tr><td><code>packed</code></td><td>FIDO2 universal format, may contain authenticator signature and X.509 certificate chain.</td></tr><tr><td><code>fido-u2f</code></td><td>Format compatible with legacy U2F security keys.</td></tr><tr><td><code>tpm</code></td><td>Attestation based on TPM.</td></tr><tr><td><code>apple</code></td><td>Anonymous attestation for Apple platform authenticators.</td></tr><tr><td><code>android-key</code> / <code>android-safetynet</code></td><td>Android platform-related attestation.</td></tr></tbody></table><div class="hint-container warning"><p class="hint-container-title">Warning</p><p>Only in enterprise/high-assurance scenarios (needing to restrict to &quot;only accept certain vendor authenticators&quot;) is it necessary to truly parse and verify attestation certificate chains. Most applications should request <code>attestation: &quot;none&quot;</code>, avoiding unnecessary privacy exposure and implementation complexity.</p></div><h2 id="ceremony-registration-and-authentication" tabindex="-1"><a class="header-anchor" href="#ceremony-registration-and-authentication"><span>ceremony: registration and authentication</span></a></h2><p>The WebAuthn specification uses <strong>ceremony (ceremony)</strong> to refer to a complete interaction process involving users, browsers, authenticators, and the RP. There are two types:</p><ol><li><strong>Registration ceremony</strong>: Creates a new credential by calling <code>navigator.credentials.create()</code>, producing attestation.</li><li><strong>Authentication ceremony</strong>: Uses an existing credential to log in by calling <code>navigator.credentials.get()</code>, producing assertion.</li></ol>`,19),o(`p`,null,[l[8]||=r(`The server-side logic of both is symmetrical: both must first issue options with a challenge, then verify the authenticator's response. Details in `,-1),n(m,{to:`/en/webauthn/flows.html`},{default:c(()=>[...l[7]||=[r(`Flows`,-1)]]),_:1}),l[9]||=r(`.`,-1)]),l[15]||=i(`<h2 id="challenge-and-replay-prevention" tabindex="-1"><a class="header-anchor" href="#challenge-and-replay-prevention"><span>challenge and Replay Prevention</span></a></h2><p><strong>challenge (Challenge)</strong> is a <strong>cryptographic random number</strong> (recommended ≥ 16 bytes) generated by the RP at the start of each ceremony, issued to the browser, and ultimately written into <code>clientDataJSON</code> and included in the authenticator&#39;s signing scope.</p><p>Replay prevention key points:</p><ul><li>challenge must be <strong>generated by the server, bound to the current session, and one-time</strong>, invalidated immediately after verification.</li><li>The RP must verify that the returned challenge <strong>byte-for-byte matches</strong> the one it issued.</li><li>Do not use predictable values (timestamps, auto-incrementing IDs) as the challenge.</li></ul><h2 id="rpid-and-origin-binding" tabindex="-1"><a class="header-anchor" href="#rpid-and-origin-binding"><span>rpId and Origin Binding</span></a></h2><p>The cryptographic foundation for phishing resistance rests on two levels of binding:</p><ul><li><strong>rpId</strong>: The identifier for the RP, must be the <strong>registrable domain</strong> of the current page origin or a subdomain. For example, when the origin is <code>https://login.example.com</code>, <code>rpId</code> can be <code>login.example.com</code> or <code>example.com</code>, but not <code>example.org</code>. The authenticator computes SHA-256 of <code>rpId</code> to get <code>rpIdHash</code>, writes it to <code>authenticatorData</code>, and binds it with the credential.</li><li><strong>origin</strong>: The browser writes the complete origin of the current page (such as <code>https://login.example.com</code>) into <code>clientDataJSON.origin</code>; this value is filled in by the <strong>browser</strong> and cannot be forged by page JavaScript.</li></ul><p>Phishing resistance principle: A phishing site <code>example.evil.com</code> cannot set <code>rpId</code> to <code>example.com</code> (fails domain matching). Even if it tricks the user, the authenticator will not return a signature usable for <code>example.com</code>. Meanwhile, when the RP verifies the signature, it will discover that <code>clientDataJSON.origin</code> is not the expected legitimate origin and reject it.</p><div class="hint-container caution"><p class="hint-container-title">Caution</p><p>Once <code>rpId</code> is set for a credential, it cannot be changed. If cross-subdomain use is needed later, use the registrable domain (such as <code>example.com</code>) rather than a specific hostname during registration, otherwise existing credentials will be unable to be used on other subdomains.</p></div><h2 id="user-verification-uv-and-user-presence-up" tabindex="-1"><a class="header-anchor" href="#user-verification-uv-and-user-presence-up"><span>user verification (UV) and user presence (UP)</span></a></h2><p>The authenticator reports two critical flags in <code>authenticatorData.flags</code>:</p><ul><li><strong>UP (User Presence)</strong>: Proves &quot;someone physically operated the authenticator&quot; (such as touching a security key). Nearly all ceremonies require UP=1.</li><li><strong>UV (User Verification)</strong>: Proves &quot;the operator is the account owner,&quot; achieved through <strong>local</strong> verification using PIN, fingerprint, facial recognition, etc. UV=1 means this is <strong>multi-factor</strong> (possessing the authenticator + biometric/PIN).</li></ul><p>The RP expresses expectations through the <code>userVerification</code> parameter: <code>required</code> (must have UV), <code>preferred</code> (attempt UV), <code>discouraged</code> (no UV needed, only UP). The RP <strong>must verify the flags according to business requirements during signature verification</strong>, not trust the request parameters.</p><h2 id="resident-key-discoverable-credential" tabindex="-1"><a class="header-anchor" href="#resident-key-discoverable-credential"><span>resident key / discoverable credential</span></a></h2><ul><li><strong>Non-resident credential</strong>: The private key is not stored in an enumerable location within the authenticator; during authentication, the RP must provide the Credential ID via <code>allowCredentials</code> to use it. Suitable for &quot;enter username first, then authenticate&quot; second-factor scenarios.</li><li><strong>Resident key / discoverable credential</strong>: The credential and user handle are stored within the authenticator and can be enumerated by the browser. Supports <strong>username-less login</strong>, the technical foundation of <strong>Passkey</strong>. Requested via <code>authenticatorSelection.residentKey</code> (<code>required</code>/<code>preferred</code>/<code>discouraged</code>).</li></ul><h2 id="signature-counter-anti-cloning" tabindex="-1"><a class="header-anchor" href="#signature-counter-anti-cloning"><span>signature counter Anti-cloning</span></a></h2><p><code>authenticatorData</code> contains a <strong>signature counter</strong> <code>signCount</code> that monotonically increments each time the authenticator signs. The RP should store the previously observed count:</p><ul><li>If the new value <strong>&gt; previous value</strong>: Normal, update storage.</li><li>If the new value <strong>≤ previous value</strong> (and not always 0): Possible <strong>credential cloning</strong>, should alert or reject.</li></ul><div class="hint-container warning"><p class="hint-container-title">Warning</p><p>Some platform authenticators (especially synced Passkeys) always return <code>signCount = 0</code>. In such cases, the counter is meaningless; the RP should not reject login based on this alone, only handle true regression scenarios like &quot;decreasing from non-zero to a smaller non-zero value.&quot;</p></div><h2 id="attestation-privacy-considerations" tabindex="-1"><a class="header-anchor" href="#attestation-privacy-considerations"><span>attestation Privacy Considerations</span></a></h2><p>If attestation certificates are <strong>unique</strong> across a batch of authenticators, they become identifiers correlatable across sites, damaging user privacy. To address this:</p><ul><li>FIDO specs require attestation certificates to cover at least 100k devices (batch attestation) to avoid single-device identification.</li><li>Platform authenticators commonly use anonymized attestation (such as Apple&#39;s anonymous CA, Android&#39;s anonymization CA).</li><li>Unless there is a specific enterprise compliance need, the RP should request <code>attestation: &quot;none&quot;</code>, preventing the browser from transmitting or stripping identifiable information.</li></ul>`,22),o(`p`,null,[l[11]||=r(`Next step: Enter `,-1),n(m,{to:`/en/webauthn/flows.html`},{default:c(()=>[...l[10]||=[r(`Registration and Authentication Flows`,-1)]]),_:1}),l[12]||=r(` to see complete ceremonies and code examples.`,-1)])])}var f=a(u,[[`render`,d]]);export{l as _pageData,f as default};