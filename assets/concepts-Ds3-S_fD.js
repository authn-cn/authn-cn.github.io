import{b as e,f as t,h as n,m as r,p as i,t as a,u as o,v as s,x as c}from"./app-D04jC65z.js";var l=JSON.parse(`{"path":"/en/oauth2/concepts.html","title":"Core Concepts","lang":"en-US","frontmatter":{"title":"Core Concepts","description":"Core Concepts This page clarifies the basic building blocks of OAuth 2.0: roles, client types, tokens, scope, endpoints, and the two critical security mechanisms of state and PK...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Core Concepts\\",\\"image\\":[\\"\\"],\\"dateModified\\":\\"2026-07-06T07:49:59.000Z\\",\\"author\\":[]}"],["meta",{"property":"og:url","content":"https://authn.tech/en/oauth2/concepts.html"}],["meta",{"property":"og:site_name","content":"Authn.tech"}],["meta",{"property":"og:title","content":"Core Concepts"}],["meta",{"property":"og:description","content":"Core Concepts This page clarifies the basic building blocks of OAuth 2.0: roles, client types, tokens, scope, endpoints, and the two critical security mechanisms of state and PK..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"en-US"}],["meta",{"property":"og:locale:alternate","content":"de-DE"}],["meta",{"property":"og:locale:alternate","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2026-07-06T07:49:59.000Z"}],["meta",{"property":"article:modified_time","content":"2026-07-06T07:49:59.000Z"}],["link",{"rel":"canonical","href":"https://authn.tech/en/oauth2/concepts.html"}],["link",{"rel":"alternate","hreflang":"de-de","href":"https://authn.tech/de/oauth2/concepts.html"}],["link",{"rel":"alternate","hreflang":"zh-cn","href":"https://authn.tech/oauth2/concepts.html"}]]},"git":{"updatedTime":1783324199000,"contributors":[{"name":"linux","username":"linux","email":"lich.wang@gmail.com","commits":1,"url":"https://github.com/linux"},{"name":"Claude Opus 4.8","username":"","email":"noreply@anthropic.com","commits":1}],"changelog":[{"hash":"3d03564154465dae2b1663376ea15a4f48fb60dd","time":1783324199000,"email":"lich.wang@gmail.com","author":"linux","message":"i18n:站点三语(中/英/德)+ 语言切换 + 浏览器语言默认","coAuthors":[{"name":"Claude Opus 4.8","email":"noreply@anthropic.com"}]}]},"filePathRelative":"en/oauth2/concepts.md","autoDesc":true}`),u={name:`concepts.md`};function d(a,l,u,d,f,p){let m=e(`RouteLink`);return s(),t(`div`,null,[l[14]||=o(`h1`,{id:`core-concepts`,tabindex:`-1`},[o(`a`,{class:`header-anchor`,href:`#core-concepts`},[o(`span`,null,`Core Concepts`)])],-1),o(`p`,null,[l[1]||=r(`This page clarifies the basic building blocks of OAuth 2.0: roles, client types, tokens, scope, endpoints, and the two critical security mechanisms of state and PKCE. After understanding these concepts, reading `,-1),n(m,{to:`/en/oauth2/flows.html`},{default:c(()=>[...l[0]||=[r(`typical flows`,-1)]]),_:1}),l[2]||=r(` will be very straightforward.`,-1)]),l[15]||=i(`<h2 id="four-roles" tabindex="-1"><a class="header-anchor" href="#four-roles"><span>Four Roles</span></a></h2><p>RFC 6749 defines four roles:</p><table><thead><tr><th>Role</th><th>Description</th><th>Example</th></tr></thead><tbody><tr><td><strong>Resource Owner</strong></td><td>An entity capable of granting access to protected resources; typically the end user</td><td>You, using the calendar application</td></tr><tr><td><strong>Client</strong></td><td>An application that requests access to protected resources on behalf of the Resource Owner</td><td>Calendar management app</td></tr><tr><td><strong>Authorization Server (AS)</strong></td><td>The server that authenticates the Resource Owner and issues tokens after obtaining consent</td><td>Google Accounts, Keycloak, Auth0</td></tr><tr><td><strong>Resource Server (RS)</strong></td><td>The server hosting protected resources, validates Access Token and provides services</td><td>Google Calendar API</td></tr></tbody></table><div class="hint-container tip"><p class="hint-container-title">Tips</p><p>Authorization Server and Resource Server can be the same system (common in small deployments) or completely separate (one AS protecting multiple APIs). &quot;Client&quot; refers to an <strong>application program</strong>, not a &quot;client device&quot; or &quot;user&quot;.</p></div><h2 id="client-types-and-client-authentication" tabindex="-1"><a class="header-anchor" href="#client-types-and-client-authentication"><span>Client Types and Client Authentication</span></a></h2><h3 id="confidential-vs-public" tabindex="-1"><a class="header-anchor" href="#confidential-vs-public"><span>Confidential vs Public</span></a></h3><table><thead><tr><th>Type</th><th>Definition</th><th>Typical Examples</th></tr></thead><tbody><tr><td><strong>Confidential Client</strong></td><td>A client capable of safely protecting credentials (such as client_secret), typically with a server component</td><td>Traditional web app backend, background services</td></tr><tr><td><strong>Public Client</strong></td><td>A client that cannot keep secrets — code/binaries are distributed to users, any secret can be extracted</td><td>SPA (JavaScript in browser), mobile apps, desktop/CLI applications</td></tr></tbody></table><p>This distinction determines security design: Public Clients <strong>should not hold a client_secret</strong> (a secret in frontend code or app package is essentially public), and must rely on mechanisms like PKCE for security.</p><h3 id="client-authentication-methods" tabindex="-1"><a class="header-anchor" href="#client-authentication-methods"><span>Client Authentication Methods</span></a></h3><p>When a Confidential Client calls the token endpoint, it must prove its identity to the AS:</p><table><thead><tr><th>Method</th><th>Mechanism</th><th>Description</th></tr></thead><tbody><tr><td><code>client_secret_basic</code></td><td>HTTP Basic auth header: <code>Authorization: Basic base64(client_id:client_secret)</code></td><td>RFC 6749 recommended default; note that id/secret must be URL-encoded first before concatenation</td></tr><tr><td><code>client_secret_post</code></td><td>Place <code>client_id</code> and <code>client_secret</code> in the request body</td><td>Compatibility option, less standardized than Basic</td></tr><tr><td><code>private_key_jwt</code></td><td>Client signs a JWT with its private key (RFC 7523), submitted as <code>client_assertion</code></td><td>Secret never leaves the client, supports key rotation, preferred for high-security scenarios (FAPI)</td></tr><tr><td>mTLS (<code>tls_client_auth</code>)</td><td>Mutual TLS, authentication with client certificate (RFC 8705)</td><td>Can also bind tokens to certificates (token binding), preventing token theft</td></tr></tbody></table><div class="hint-container tip"><p class="hint-container-title">Selection Recommendation</p><p>For general web backends, <code>client_secret_basic</code> is sufficient; for high-security scenarios (open banking, business integration), prioritize <code>private_key_jwt</code> or mTLS to avoid sharing symmetric keys.</p></div><h2 id="tokens" tabindex="-1"><a class="header-anchor" href="#tokens"><span>Tokens</span></a></h2><h3 id="access-token" tabindex="-1"><a class="header-anchor" href="#access-token"><span>Access Token</span></a></h3><p>The credential presented by the client when accessing the Resource Server. Key points:</p><ul><li><strong>Bearer semantics</strong> (RFC 6750): &quot;Bearer token&quot; — <strong>whoever obtains it can use it</strong>, no verification of the presenter&#39;s identity. This is why leak prevention (TLS throughout, not in logs, not in URLs) is so critical.</li><li><strong>Short validity period</strong>: Typically 5 minutes to 1 hour, renewed via Refresh Token.</li><li><strong>Limited permissions</strong>: Only covers the scope approved during authorization.</li></ul><h3 id="refresh-token" tabindex="-1"><a class="header-anchor" href="#refresh-token"><span>Refresh Token</span></a></h3><p>Used to <strong>obtain a new Access Token</strong> after the Access Token expires, avoiding repeated user logins:</p>`,18),o(`ul`,null,[l[8]||=o(`li`,null,[r(`Only issued to the client, only used against the AS's token endpoint, `),o(`strong`,null,`never sent to the Resource Server`),r(`;`)],-1),l[9]||=o(`li`,null,`Long validity period (days to months), therefore more valuable and must be stored securely;`,-1),o(`li`,null,[l[4]||=r(`Modern practice requires `,-1),l[5]||=o(`strong`,null,`rotation for Public Clients`,-1),l[6]||=r(`: each use issues a new Refresh Token and invalidates the old one. Reuse of an old token indicates a leak, triggering revocation of the entire token family (see `,-1),n(m,{to:`/en/oauth2/flows.html#refresh-token-flow`},{default:c(()=>[...l[3]||=[r(`flows page`,-1)]]),_:1}),l[7]||=r(`).`,-1)])]),l[16]||=i(`<h3 id="opaque-vs-jwt" tabindex="-1"><a class="header-anchor" href="#opaque-vs-jwt"><span>opaque vs JWT</span></a></h3><table><thead><tr><th>Format</th><th>Validation</th><th>Advantages</th><th>Disadvantages</th></tr></thead><tbody><tr><td><strong>Opaque</strong></td><td>RS calls AS&#39;s introspection endpoint (RFC 7662) to query</td><td>Can be revoked immediately; doesn&#39;t leak internal information</td><td>Network overhead on every validation</td></tr><tr><td><strong>JWT (self-contained)</strong></td><td>RS verifies locally with AS&#39;s public key (RFC 9068 specifies JWT Access Token format)</td><td>No network overhead, suitable for high throughput/microservices</td><td>Cannot be revoked immediately after issuance, relies on short expiry</td></tr></tbody></table><div class="hint-container warning"><p class="hint-container-title">Warning</p><p>The format of an Access Token is an agreement between the AS and RS. <strong>Clients should not parse Access Token contents</strong>, even if it happens to be JWT — treat it as an opaque string. For user information, use OIDC&#39;s ID Token or UserInfo endpoint.</p></div><h2 id="scope-permission-range" tabindex="-1"><a class="header-anchor" href="#scope-permission-range"><span>Scope (Permission Range)</span></a></h2><p><code>scope</code> is a space-delimited list of strings expressing the permission range the client requests, for example:</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">scope=calendar.read calendar.write profile</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><ul><li>The AS can <strong>reduce</strong> the scope the client requests (user denies some permissions), and the actually granted scope will be returned in the token response;</li><li>Design principle: <strong>least privilege</strong>, read/write separation (<code>resource.read</code> / <code>resource.write</code>), appropriate granularity (too fine-grained makes the authorization screen unreadable, too coarse grants excessive permissions);</li><li>scope describes &quot;what the client can do&quot;, not &quot;what the user can do&quot; — the RS still needs to apply its own user-level permission checks.</li></ul><h2 id="redirect-uri-and-exact-matching" tabindex="-1"><a class="header-anchor" href="#redirect-uri-and-exact-matching"><span>redirect_uri and Exact Matching</span></a></h2><p><code>redirect_uri</code> is the address where the AS sends the authorization code after authorization is complete. <strong>It is critical to the security of the authorization code flow</strong>: if an attacker can cause the AS to send code to an address they control, they can steal the authorization.</p><div class="hint-container caution"><p class="hint-container-title">Must Match Exactly</p><p>When clients register, they must provide the complete redirect_uri, and the value in the authorization request must match the registered value <strong>character-for-character exactly</strong> (RFC 9700 / OAuth 2.1 mandate this). Prohibited:</p><ul><li>Prefix matching, subdomain wildcards (<code>https://*.example.com/cb</code>);</li><li>Validating only the domain, not the path;</li><li>Allowing arbitrary query parameters on <code>redirect_uri</code> and then doing &quot;fuzzy matching&quot;.</li></ul><p>Historically, many major OAuth vulnerabilities (open redirect → authorization code leak) have stemmed from loose redirect_uri validation.</p></div><h2 id="endpoints" tabindex="-1"><a class="header-anchor" href="#endpoints"><span>Endpoints</span></a></h2><table><thead><tr><th>Endpoint</th><th>Called by</th><th>Purpose</th><th>Definition</th></tr></thead><tbody><tr><td><strong>Authorization Endpoint</strong></td><td>User&#39;s browser (front channel)</td><td>User login, confirm authorization, issue authorization code</td><td>RFC 6749</td></tr><tr><td><strong>Token Endpoint</strong></td><td>Client (back channel, server-to-server)</td><td>Exchange authorization code/Refresh Token/client credentials for tokens</td><td>RFC 6749</td></tr><tr><td><strong>Revocation Endpoint</strong></td><td>Client</td><td>Actively invalidate Access/Refresh Tokens no longer needed (e.g., user logout, app uninstall)</td><td>RFC 7009</td></tr><tr><td><strong>Introspection Endpoint</strong></td><td>Resource Server (requires authentication)</td><td>Query whether token is valid and its metadata (scope, expiration, owner user)</td><td>RFC 7662</td></tr></tbody></table><p>Endpoint addresses can be auto-discovered via <strong>AS Metadata</strong> (RFC 8414): request <code>https://as.example.com/.well-known/oauth-authorization-server</code> to get a JSON configuration document, avoiding hardcoding.</p><h2 id="state-parameter-and-csrf-protection" tabindex="-1"><a class="header-anchor" href="#state-parameter-and-csrf-protection"><span>state Parameter and CSRF Protection</span></a></h2><p><code>state</code> is a random, unguessable value that the client includes in the authorization request; the AS returns it unchanged in the callback. The client must verify that the <code>state</code> in the callback matches the value saved in its session.</p><p>It defends against the attack (login CSRF / session confusion): an attacker uses <strong>their own</strong> authorization code to construct a callback URL to trick a victim into visiting it, causing the victim&#39;s client session to be silently bound to the attacker&#39;s account. Subsequent data written by the victim (such as file uploads, payment method binding) falls into the attacker&#39;s account.</p><p>Requirements:</p><ul><li>Generate a new, cryptographically random <code>state</code> (≥128 bits of entropy) on each authorization request, bound to the user session;</li><li>When calling back, <strong>verify state first, abort immediately if it doesn&#39;t match</strong>;</li><li>After using PKCE, PKCE can also cover such attack surfaces, but <code>state</code> is still recommended (can also serve anti-replay and context recovery purposes; if using it to carry business state, only store non-forgeable references rather than plaintext data).</li></ul><h2 id="pkce" tabindex="-1"><a class="header-anchor" href="#pkce"><span>PKCE</span></a></h2><p><strong>PKCE</strong> (Proof Key for Code Exchange, RFC 7636, pronounced &quot;pixy&quot;) adds dynamic proof to authorization code exchange, defending against <strong>authorization code interception attacks</strong>.</p><h3 id="principle" tabindex="-1"><a class="header-anchor" href="#principle"><span>Principle</span></a></h3><ol><li>Before each authorization, the client generates a random string <code>code_verifier</code> (43–128 characters, cryptographically random);</li><li>Computes <code>code_challenge = BASE64URL(SHA256(code_verifier))</code>, the <strong>S256</strong> method;</li><li>The authorization request includes <code>code_challenge</code> and <code>code_challenge_method=S256</code>; the AS stores these bound to the issued authorization code;</li><li>When the client exchanges the authorization code for a token, it submits the original <code>code_verifier</code>;</li><li>The AS performs the same SHA256 operation on the received verifier, compares it to the previously stored challenge; if mismatched, it rejects.</li></ol><p>Even if an attacker intercepts the authorization code (malicious app registering the same custom URL scheme, system log leak, browser history), they lack the corresponding <code>code_verifier</code> and cannot complete the exchange.</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">code_verifier  = dBjftJeZ4CVP-mB92K27uhbUJU1p1r_wW1gFWFOEjXk   (randomly generated, only known to client)</span>
<span class="line">code_challenge = E9Melhoa2OwvFrEMTJguCHaoeK1t8URWbuGJSstw-cM   (after SHA256 base64url)</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container warning"><p class="hint-container-title">Do Not Use plain Method</p><p><code>code_challenge_method=plain</code> (challenge equals verifier directly) is only a legacy compatibility fallback option, losing one-way protection. Always use <strong>S256</strong>.</p></div><h3 id="why-recommended-for-all-clients" tabindex="-1"><a class="header-anchor" href="#why-recommended-for-all-clients"><span>Why Recommended for All Clients</span></a></h3><p>PKCE was originally designed for Public Clients like mobile apps, but <strong>RFC 9700 and OAuth 2.1 require all clients using authorization code mode (including Confidential Clients holding client_secret) to use PKCE</strong>, for reasons:</p><ul><li>client_secret can only prove &quot;this is that client&quot;, not bind <strong>this authorization code</strong> to <strong>this request</strong>; PKCE provides transaction-level binding;</li><li>PKCE defends against authorization code injection (attacker injects stolen code into victim&#39;s callback) — this attack is equally effective against Confidential Clients;</li><li>Implementation cost is minimal (two hash operations), enabling it universally has no burden.</li></ul><h2 id="next-steps" tabindex="-1"><a class="header-anchor" href="#next-steps"><span>Next Steps</span></a></h2>`,29),o(`ul`,null,[o(`li`,null,[n(m,{to:`/en/oauth2/flows.html`},{default:c(()=>[...l[10]||=[r(`Typical Flows`,-1)]]),_:1}),l[11]||=r(`: See how these concepts combine into complete authorization flows`,-1)]),o(`li`,null,[n(m,{to:`/en/oauth2/reference.html`},{default:c(()=>[...l[12]||=[r(`Typical Parameters and Response Reference`,-1)]]),_:1}),l[13]||=r(`: Parameter and error code quick lookup for each endpoint`,-1)])])])}var f=a(u,[[`render`,d]]);export{l as _pageData,f as default};